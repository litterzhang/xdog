[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "xdog-site"
version = "2.3.0"
description = "xdog public site: package overview, blog, and FAQ (Flask)"
requires-python = ">=3.12"
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries",
    "Typing :: Typed",
]
authors = [{ name = "HugeMan" }]
license = "AGPL-3.0-or-later"
license-files = ["LICENSE", "LICENSE-EXCEPTION.md"]
dependencies = [
    "flask>=3.0",
    "markdown>=3.5",
    "python-frontmatter>=1.0",
    "pygments>=2.17",
    "python-frontmatter>=1.0",
    "markupsafe>=2.1",
]

[project.urls]
Homepage = "https://xdog.942295.xyz"
Repository = "https://github.com/litterzhang/xdog"
Documentation = "https://xdog.942295.xyz"
Issues = "https://github.com/litterzhang/xdog/issues"

[project.scripts]
xdog-site = "xdog.site.app:main"

[tool.hatch.build.targets.wheel]
packages = ["src/xdog"]

[tool.mypy]
python_version = "3.12"
strict = true
warn_return_any = true
warn_unused_configs = true

[[tool.mypy.overrides]]
module = [
    "flask", "flask.*", "jinja2", "jinja2.*", "markupsafe", "markupsafe.*", "flow", "flow.*",
    "markdown", "markdown.*", "frontmatter", "frontmatter.*",
]
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
